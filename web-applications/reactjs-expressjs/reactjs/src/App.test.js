import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

it("renders without crashing", () => {
  // const div = document.createElement('div');
  // ReactDOM.render(<App />, div); # Will fail because the Fibonnacci component is making a request to a resource. TODO need to mock these requests
  // ReactDOM.unmountComponentAtNode(div);
});
